# -------------------------------------------------------------------------------
# Reload after changing...
#
# says it doesn't recognize /towny commands but it does...

# [20:29:37 WARN]: Could not register alias home because it contains commands that do not exist: /towny:resident spawn
# [20:29:37 WARN]: Could not register alias reload because it contains commands that do not exist: /townyadmin reload all
# bukkit:rl commands
# -------------------------------------------------------------------------------

command-block-overrides: []
ignore-vanilla-permissions: false
aliases:
  icanhasbukkit:
  - version $1-
  # icanhasbukkit:
  # - version $1-
  home:
  - /towny:resident spawn
  prices:
  - towny prices
  reload:
  - /townyadmin reload all
